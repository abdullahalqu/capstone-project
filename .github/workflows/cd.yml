# name: Deploy Application on AliCloud Infra

# on:
#   push:
#     branches: main

# jobs:
#   ssh:
#     runs-on: self-hosted
#     strategy:
#       matrix:
#         ips: [10.0.1.21, 10.0.1.13]
#     steps:
#       - 
#         name: Create SSH key file
#         run: |
#           mkdir -p ~/.ssh &&
#           echo "${{ secrets.SSH_KEY }}" > ~/.ssh/temp_key &&
#           chmod 600 ~/.ssh/temp_key

#       - 
#         name: Add SSH Host to Known Hosts
#         run: |
#           ssh-keyscan -H ${{ matrix.ips }} >> ~/.ssh/known_hosts

#       - 
#         name: Pull new build
#         run: |
#           ssh -o StrictHostKeyChecking=no -i ~/.ssh/temp_key ${{ secrets.SSH_USERNAME }}@${{ matrix.ips }} "
#           cd /docker-compose-flask-app &&
#           docker pull "
